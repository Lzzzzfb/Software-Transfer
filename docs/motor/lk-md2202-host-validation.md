# LK-MD2202 主机侧验证记录

记录更新日期：2026-08-17。自动化记录只证明可由主机测试覆盖的代码路径；下方
另列用户已经完成的 ROCK 4B+ 实机观察和尚待复验项目。

## 已完成

- Modbus-RTU CRC、分帧、严格响应解析和异常响应测试；
- LK-MD2202 身份、寄存器、默认参数和 32 位高低寄存器测试；
- USB 串口候选枚举与只读协议身份确认；
- 读取可重试、动作不重试、紧急停止队列和有限关闭；
- X/M1、Y/M2 的 320 pulse/mm、软件零点、回零和行程保护；
- 电机设置弹窗、建议默认值、固定零点限位和参数差异摘要；
- `n+1` 次 X、`n` 次 Y、整数脉冲子步、步时、多轮采集/保存/返回；
- 扫描占用、停止与故障清理；
- ROCK 4B+ 部署源码镜像检查。
- LK-MD2202速度控制寄存器的正数、负数、0和int32边界编码；
- “设速”取绝对值、“脱离卡死”保留方向符号的会话语义；
- 限位闭合→断开提前停止、错误方向断开→闭合停止和1秒/4800 pulse双重截止；
- 机械回零必须观察本次断开→闭合，并使用2～5秒动态超时；
- 速度0、位置急停、停止回读、停止未知锁定和两轴停止后解锁；
- 空闲500 ms状态轮询、三态限位显示、`ttyFIQ*`排除和连接状态栏结束提示；
- 停止未知只锁定电机与扫描，不锁定独立光谱仪采集。
- 电机动作开始时的焦点处理，不再把焦点转移到“X行程”；
- 脱离卡死速度0后的有界状态复核、一次位置急停兜底和持续未知时安全锁；
- 无光谱仪时的纯电机扫描、多轮返回、停止和故障状态机；
- 纯电机扫描不创建独立清单，结构化事件进入当前诊断时间线并可随诊断包导出；
- 有光谱仪时原连续采集、停止保存、返回和扫描清单路径保持不变。

2026-08-17 本轮结果：电机测试 `135 passed`；电机控制器、面板、扫描与诊断
定向测试 `58 passed`；最终完整回归 `435 passed`；部署结构测试 `6 passed`。
ROCK 4B+ 部署源码共 93 个文件且与主源码一致。测试中的5条 pyqtgraph 弃用警告
与上一版相同，不影响本轮结果。自动测试不能替代板端实机运动验收。

## 已完成的 ROCK 4B+ 实机观察

- Debian 12 Bookworm ARM64、Python 3.11 环境可运行现有软件；
- CH341 USB串口驱动已加载，稳定路径映射到 `/dev/ttyUSB0`；
- LK-MD2202 地址1、9600 baud、8N1只读探针成功；
- X/Y参数设置及正反向运动正常；
- 机械回零能够识别零点限位闭合并停止；
- 回零卡在机械零点后，“脱离卡死”能够让电机脱离。

## 当前验证环境

- Windows 测试解释器：Python 3.13.7；
- Windows 完整回归 Qt：PyQt6 6.11，QtSerialPort 来自同一绑定；
- 产品依赖与部署目标：Debian 12 ARM64 官方 PyQt6；
- ROCK 4B+ 仍须完成真实桌面、USB-RS485、电机运动、限位和光谱仪联动验收。

## 本轮尚待实机复验

- 电机动作结束后“X行程”不再自动获得焦点；
- 脱离卡死停止状态延迟不会误触发重连锁定，确认停止后可直接机械回零；
- 真正无法确认停止时安全锁仍然生效；
- 不连接光谱仪时的短行程纯电机扫描、分步、步时、多轮和返回；
- 纯电机扫描诊断日志显示与诊断包导出；
- 连接光谱仪后的多轮联动扫描与真实光谱数据完整性；
- X/Y 的 1、5、15 mm 实际距离、长时间运行和负载温升。

执行顺序见 `rock4bplus-acceptance-checklist.md`。未完成项目不得标记为验收通过。
