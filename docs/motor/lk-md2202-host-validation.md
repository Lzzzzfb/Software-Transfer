# LK-MD2202 主机侧验证记录

记录日期：2026-08-10。此记录只证明不接真实机构时可验证的主机代码，不代表
ROCK 4B+、RS-485 电气、方向、限位、距离、温升或负载验收。

## 已完成

- Modbus-RTU CRC、分帧、严格响应解析和异常响应测试；
- LK-MD2202 身份、寄存器、默认参数和 32 位高低寄存器测试；
- USB 串口候选枚举与只读协议身份确认；
- 读取可重试、动作不重试、紧急停止队列和有限关闭；
- X/M1、Y/M2 的 320 pulse/mm、软件零点、回零和行程保护；
- 电机设置弹窗、建议默认值、固定零点限位和参数差异摘要；
- `n+1` 次 X、`n` 次 Y、整数脉冲子步、步时、多轮采集/保存/返回；
- 扫描占用、停止与故障清理；
- ROCK 4B+ 部署源码镜像检查。

自动化结果：`368 passed, 1 skipped`。跳过项是当前 Windows 环境未安装
PyQtGraph 的可选后端测试；ROCK 4B+ 安装脚本会安装并断言 PyQtGraph 实际使用
PyQt6，因此该项必须在板端重新执行，不能视为已经验收。

## 当前验证环境

- Windows 测试解释器：Python 3.9.13；
- 当前可用兼容 Qt：PyQt5；
- 产品依赖与部署目标：PyQt6；
- PyQt6 必须在 ROCK 4B+ 或单独的 PyQt6 环境再次运行全部测试和界面冒烟测试。

## 尚待硬件

- USB-RS485 身份、驱动、权限和稳定路径；
- LK-MD2202 实际身份/配置/状态响应样本；
- M1-K、M2-K 闭合接地逻辑；
- X/Y 真实正方向、回零方向和方向反转；
- 1、5、15 mm 位移与 320 pulse/mm；
- 卡滞、掉步、断线和急停；
- 多轮扫描与真实光谱数据完整性。

执行顺序见 `rock4bplus-acceptance-checklist.md`。未完成项目不得标记为验收通过。
