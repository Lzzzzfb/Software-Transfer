# 实机联调与发布环境待验证清单

状态：2026-07-23 物理重新插拔后，COM14/COM17/COM18 已全部恢复并完成本轮新控制、界面和异步存储功能的三机实机回归。普通采集链路已完成验证，余项只涉及特定光源、硬同步接线、异常介质和正式发布环境。详见 [2026-07-23 联调报告](hardware-validation-report-2026-07-23.md)。

## 1. 协议和连接

- [x] 确认 40 字节版本参数、U16LE 实机包号、U16BE 像素及两种数据帧格式兼容。
- [x] 三台设备 27 组设置命令均返回 `0x61` 并回读一致。
- [x] 程序化关闭串口后的 1 秒退避重连验证。
- [x] Python 3.13/PySide6 6.11 下自动排除无协议响应的串口，并识别 COM14/COM17/COM18。
- [x] 实际物理重新插拔后，确认 Windows 串口恢复且三台设备重新完成协议握手。

## 2. 参数与校准

- [x] 参数 ACK 后生效；采集中禁止穿插参数命令。
- [x] 2052 总像素/2048 有效像素及 3694/3648 设备的起始像素裁剪正确。
- [ ] 用已知谱线光源验证绝对波长；当前系数已保留且未改写。

## 3. 采集与同步

- [x] 三台单帧、连续和软件同步采集；历史 500 帧实机批次零缺帧。
- [x] 新总控状态机完成三机 10 轮控制、三卡并行交错停止、冲突守卫和异步停止。
- [x] 三台设备最终存储会话各 10 帧，帧号零缺失、零重复、零乱序。
- [x] 上位机不在逐帧路径发送 `0x54`。
- [ ] 指定物理同步主机和接线方向后，验证内部硬同步。
- [ ] 接入外部触发源后，验证外部硬同步。
- [ ] 提供第四台设备及可达 100 fps 的链路后执行四机压力测试。

## 4. 处理和存储

- [x] 不同波段/像素数三机导出到独立 Excel 工作表及各自校准波长列。
- [x] 新规范文件名、尾批导出、后台收尾和固定 Y 完成三机实机验证。
- [x] 新总控 Excel 同时包含 `001`、`002`、`003` 三个设备工作表。
- [x] 两个实机批次的 Excel/CSV 均按每帧一列、每像素一行通过结构核对。
- [x] 新鲜全局背景和三台设备各自的新鲜参考采集完成控制链路验证。
- [ ] 使用真实暗场、参考场和样品核对扣背景与吸光度物理结果。
- [ ] 人为占用 Excel 文件、断开磁盘或制造空间不足，确认现场错误提示和 `.part` 保留。
- [ ] 异常断开设备后运行恢复流程，核对截断尾记录报告。

## 5. 64 位发布

- 当前项目 `.venv` 已安装并验证 PySide6 6.11；尚未安装 PyInstaller，也未生成正式发布目录。
- 需在确定的 64 位发布环境运行 `python -m PyInstaller --clean --noconfirm spectrometer.spec`。
- 在干净 Windows 10/11 64 位机器确认 Qt 平台插件、Qt SerialPort、CSV/Excel、中文路径和安全退出。
- 确认程序关闭后无残留串口线程、存储线程或 `.part`（存在导出错误时除外）。

联调原始串口日志、设备型号/固件版本、截图和导出文件保存在不提交 Git 的 `data/validation/`。
